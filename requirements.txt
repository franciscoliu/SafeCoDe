# SafeCoDe runtime dependencies.
#
# These are minimum versions known to work. For the exact pins used to produce
# the numbers in the paper, see requirements-lock.txt.
#
#   pip install -r requirements.txt

torch>=2.4.0
transformers>=4.57.0        # Qwen3-VL support; drop to >=4.49 for Qwen2.5-VL only
accelerate>=1.0.0           # required by device_map="auto"
datasets>=3.0.0             # HADES and MSTS stream from the Hugging Face hub
numpy>=1.26.0
pandas>=2.0.0
pillow>=10.0.0
requests>=2.31.0
tqdm>=4.66.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# Needed only for --model_type qwen. Imported lazily, so the other three
# architectures run without it.
qwen-vl-utils>=0.0.11
